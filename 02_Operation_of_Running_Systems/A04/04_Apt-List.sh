apt list

apt list zz*

apt list zzuf

apt list --installed

apt list --installed | wc -l

apt list --upgradable

sudo apt upgrade ansible

apt show ansible



Using apt-list we can list all packeges, or just installed, upgradeable or available, as
well as by pattern. To see information on packages we can read the metadata using
apt show.