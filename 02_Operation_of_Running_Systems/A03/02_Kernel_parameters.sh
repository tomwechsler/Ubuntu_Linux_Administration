init=/bin/bash
passwd
exec /sbin/init



We can pass parameters to the Kernel that can help us recover from issues. For example, if we
have not set the root password and sudo has become unusable we are able to reset or recover 
the root user`s password.